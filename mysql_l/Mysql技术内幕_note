	高效查询:
		整理表碎片: 频繁修改的表, 尤其是包含可变长度数据列的表, 会产生大量的碎片;
		具体操作: 先使用mysqldump转储表, 然后再利用这个转储文件重建它;
	
	当创建InnoDB表时, 选择特性与存储在表中的数据完美匹配的行存储格式:
		1) 默认情况下, InnoDB会使用COMPACT行格式;
		2) 对于包含重复数据的表, 使用COMPRESSED行格式;
		3) 对于带有长BLOB或TEXT值的表, DYNAMIC行格式最有效;


